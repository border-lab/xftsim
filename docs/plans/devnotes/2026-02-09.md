# 2026-02-09 Next Steps Planning

## Session Focus
- Wrote comprehensive brainstorm/plan document for Ajay: `docs/plans/next-steps-brainstorm.md`
- Explored submodules: stdpopsim, msprime, grgl, grapp

## Key Findings from Submodule Exploration
- **stdpopsim**: `get_species()` → `get_contig()` → `engine.simulate()` → `tskit.TreeSequence`
- **msprime**: `sim_ancestry()` + `sim_mutations()` → `TreeSequence` with `.genotype_matrix()`
- **grgl**: `grg convert input.trees output.grg` for tree-seq → GRG; `pygrgl.load_immutable_grg()` + `pygrgl.matmul()` for ops
- **grapp**: Analysis tools (GWAS, PCA, filter, export) built on GRG — not for founder generation

## Plan Document Summary
35 tasks organized into 8 categories and 5 priority phases:
- Phase A (foundation): tree_sequence_to_haplotypes, recombination_map converters, founders_from_msprime
- Phase B (stdpopsim): founders_from_stdpopsim, genetic map integration, validation tests
- Phase C (GRG pipeline): programmatic tree-seq → GRG conversion
- Phase D (I/O): PLINK BED + VCF read/write
- Phase E (stretch): multi-chromosome, DFE, GxE, grapp integration
